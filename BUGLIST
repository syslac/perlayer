* /tmp/files creation not working, or at least not updating percentage (FIXED - confirm pending)
* Tags should be added on-the-fly, not at next song (as votes do) (FIXED - CLOSED)
* Keep current volume at song switch if replaygain tag is not set (FIXED - confirm pending)
* On crash, call cleanup; at least set ReadMode back to 1 (FIXED - confirm pending)
* Get album and track mode to use totally different callbacks
* Notice when files changed and rebuild database if they did.

